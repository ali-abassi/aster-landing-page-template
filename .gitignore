.DS_Store
node_modules/

# Keep the optimized alpha masters in the repository; the much larger
# generation masters stay local.
assets/*.png

# Evidence is committed as compressed JPEG. Raw captures stay local.
evidence/*.png
